import AdminModulePlaceholder from '@/components/layout/AdminModulePlaceholder';

export const metadata = {
  title: 'חופשות | ישיבת חדוותא',
};

export default function LeavesPage() {
  return <AdminModulePlaceholder title="חופשות" />;
}
